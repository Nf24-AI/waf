# تفاصيل مشروع Meeting Prep Tool

## 1. الملخص التنفيذي

المشروع عبارة عن مساحة عمل عربية/إنجليزية لتحضير الاجتماعات وعرضها، مبنية كتطبيق **Full-stack** وليس واجهة ثابتة فقط. الواجهة الأمامية مسؤولة عن التحضير والتفاعل والعرض، بينما يوفّر الخادم المصادقة ومسارات API آمنة والتكامل مع Notion، مع وجود مصدر بيانات محلي احتياطي حتى تبقى النسخة قابلة للاستخدام قبل الاتصال الخارجي.

النسخة الحالية منشورة على نطاق المشروع:

`https://meetprep-tjznvlsy.manus.space`

وآخر نسخة محفوظة في المشروع هي `2a50f10d`.

## 2. التقنيات المستخدمة

| الطبقة | التقنية | الاستخدام |
|---|---|---|
| Frontend | React 19 + TypeScript | بناء الواجهة والمكونات التفاعلية |
| Build tooling | Vite 7 | التطوير المحلي وتجميع الواجهة |
| Styling | Tailwind CSS 4 + CSS مخصص | نظام الألوان والتخطيط والاستجابة |
| Navigation | Wouter | مسارا التحضير والعرض |
| API contract | tRPC 11 | اتصال typed بين الواجهة والخادم |
| Backend | Express 4 + Node.js | تشغيل الخادم ومسارات التطبيق |
| Database layer | Drizzle ORM + MySQL/TiDB | طبقة قاعدة البيانات والمستخدمين |
| Authentication | Manus OAuth | تسجيل الدخول والجلسات |
| External data | Notion REST API | قراءة الاجتماعات وحفظ التغييرات |
| Testing | Vitest + Testing Library + jsdom | اختبارات الخادم وتفاعل الواجهة |
| Icons and feedback | Lucide React + Sonner | الأيقونات والتنبيهات |

## 3. هيكل التطبيق

```text
meeting-prep-tool/
├── client/
│   ├── index.html
│   └── src/
│       ├── App.tsx
│       ├── index.css
│       ├── pages/Home.tsx
│       ├── pages/Home.test.tsx
│       ├── components/
│       ├── contexts/
│       └── lib/trpc.ts
├── server/
│   ├── routers.ts
│   ├── notion.ts
│   ├── db.ts
│   ├── notion.connection.test.ts
│   ├── meeting.utilities.test.ts
│   ├── meeting.routes.test.ts
│   └── _core/
├── shared/
│   ├── meeting.ts
│   ├── meeting-store.ts
│   └── routes.ts
├── drizzle/
│   └── schema.ts
├── project-details-ar.md
└── package.json
```

## 4. ما تم بناؤه في الواجهة الأمامية

الواجهة مصممة باتجاه RTL افتراضيًا، مع إمكانية التحويل إلى LTR عند اختيار الإنجليزية. تم تقليل الزخرفة والاعتماد على تخطيط عملي مناسب للاستخدام اليومي، مع الحفاظ على اختلاف واضح بين **وضع التحرير** و**وضع العرض**.

### لوحة الاجتماعات

تحتوي الصفحة الرئيسية على قائمة الاجتماعات، مربع بحث، اجتماع نشط، إحصاءات سريعة، وقوالب بدء سريعة. يستطيع المستخدم اختيار الاجتماع الذي يريد تحضيره، ومعرفة حالته، ثم الانتقال مباشرة إلى العرض.

### وضع التحرير

يتضمن وضع التحرير الأقسام التالية:

| القسم | الوظيفة |
|---|---|
| المعلومات الأساسية | عنوان الاجتماع، التاريخ، الوقت، النوع، والحضور |
| الملخص التنفيذي | النتيجة أو القرار المطلوب من الاجتماع |
| الملاحظات التحضيرية | ملاحظات خاصة بالمستخدم قبل الاجتماع |
| مواضيع النقاش | محاور الاجتماع مع السياق والهدف لكل محور |
| المرجع الخارجي | رابط Notion أو رابط المادة المرتبطة بالاجتماع |
| نقاط المتابعة | المهام أو الخطوات التي يجب تثبيتها أثناء الاجتماع |

تتوفر أزرار إضافة وحذف وإعادة ترتيب لمحاور النقاش ونقاط المتابعة. كما يظهر مؤشر الحفظ، وحالة اتصال Notion، ورسائل الخطأ وإعادة المحاولة.

### وضع العرض

وضع العرض مخصص للمشاركة على الشاشة دون عناصر تحرير. يعرض عنوان الاجتماع، نوعه، تاريخه، ملخص الهدف، محاور النقاش المرقمة، الحضور، وتذييلًا بسيطًا. تمت إضافة اختصار المحتوى الطويل حتى لا يدفع النص الصفحة إلى تمرير غير مرغوب أثناء العرض، مع إمكانية فتح الوضع بملء الشاشة.

### اللغتان

يوجد زر واضح للتبديل بين العربية والإنجليزية. لا يغيّر الزر النصوص فقط، بل يغيّر اتجاه المستند أيضًا من RTL إلى LTR في وضع الإنجليزية. تمت ترجمة العناوين والأزرار وحالات التحميل والاتصال والحقول الأساسية.

## 5. تخصيص الواجهة UI

أضيفت لوحة تخصيص داخل التطبيق، وتفتح من أيقونة الإعدادات في الشريط العلوي. الاختيارات لا تحتاج إلى قاعدة بيانات؛ تحفظ محليًا في المتصفح تحت المفتاح `meeting-prep-ui` وتطبّق في التحضير والعرض.

| الإعداد | الخيارات |
|---|---|
| المظهر | فاتح، رملي، ليلي |
| اللون الرئيسي | Sage، Blue، Plum |
| كثافة المحتوى | مريح، مضغوط |
| كثافة العرض | قياسي، مضغوط |
| الإرجاع | إعادة الإعدادات الافتراضية |

يستطيع المستخدم تجربة لوحة الإعدادات مباشرة عبر المسار الاختياري `/?customize=1`. أما العرض المضغوط فيمكن معاينته عبر `/display?displayDensity=compact`.

## 6. طبقة الخادم وواجهات API

الاتصال بين الواجهة والخادم يتم عبر tRPC، لذلك تنتقل أنواع البيانات من تعريف الإجراء في الخادم إلى استدعاء الواجهة دون بناء طبقة REST منفصلة.

| الإجراء | النوع | الحماية | الوظيفة |
|---|---|---|---|
| `auth.me` | Query | عام | إرجاع المستخدم الحالي إن وجد |
| `auth.logout` | Mutation | عام | حذف جلسة المصادقة |
| `meetings.notionList` | Query | محمي | قراءة اجتماعات قاعدة Notion |
| `meetings.notionUpdate` | Mutation | محمي | حفظ اجتماع كامل في Notion |

الإجراء `meetings.notionUpdate` يتحقق من بنية البيانات بواسطة Zod، بما في ذلك العنوان والتاريخ والوقت والنوع والحالة والحضور والملخص والمحاور ونقاط المتابعة والملاحظات والرابط.

## 7. تكامل Notion

تم إنشاء قاعدة Notion باسم **Meeting Prep** وربطها بتكامل المشروع. يحتوي الاتصال الآمن على متغيرين بيئيين:

| المتغير | الاستخدام |
|---|---|
| `NOTION_API_TOKEN` | رمز التكامل السري للوصول إلى Notion |
| `NOTION_DATABASE_ID` | معرّف قاعدة Meeting Prep |

معرّف القاعدة المستخدم حاليًا هو:

`3c7fbc2f6932801d83e4c73969a72284`

يتعامل الخادم مع Notion API مباشرة ولا يرسل التوكن إلى الواجهة الأمامية. عند القراءة، يتم تحويل خصائص Notion إلى نموذج الاجتماع الداخلي. الخصائص المدعومة هي `Name` و`Date` و`Type` و`Attendees` و`Status` و`Summary` و`External link`.

بالنسبة للمحتوى التفصيلي داخل صفحة الاجتماع، يستخدم المحول أقسامًا داخلية ثابتة في محتوى الصفحة:

| قسم Notion | البيانات |
|---|---|
| `Time` | وقت الاجتماع |
| `Agenda` | محاور النقاش مع العنوان والسياق والهدف |
| `Actions` | نقاط المتابعة |
| `Note` | الملاحظات التحضيرية |

عند الحفظ، يتم تحديث خصائص الصفحة ثم استبدال كتل المحتوى التفصيلي بمحتوى الاجتماع الحالي. هذا يجعل Notion مصدر الحقيقة للاجتماعات الموجودة فيه، مع بقاء المصدر المحلي الاحتياطي متاحًا في حال عدم الاتصال.

## 8. نموذج بيانات الاجتماع

النموذج المشترك موجود في `shared/meeting-store.ts`، ويستخدم في الخادم والواجهة معًا.

```ts
type MeetingRecord = {
  id: string;
  title: string;
  date: string;
  time: string;
  type: string;
  status: "مسودة" | "جاهز للعرض" | "تم الاجتماع";
  attendees: string[];
  summary: string;
  agenda: Array<{
    title: string;
    context: string;
    goal: string;
  }>;
  actions: string[];
  note: string;
  link: string;
};
```

يوجد مصدر محلي من نوع `MeetingSource` يستخدم للاختبار والعمل الاحتياطي قبل توفر بيانات Notion. هذا الفصل يجعل استبدال مصدر البيانات أو إضافة مصدر آخر أسهل دون إعادة بناء الواجهة بالكامل.

## 9. القوالب الجاهزة

تم توفير أربعة قوالب بالعربية والإنجليزية:

| القالب | الاستخدام |
|---|---|
| اجتماع داخلي / Internal sync | تحديثات الفريق والأولويات |
| اجتماع شريك / Partner review | الأهداف المشتركة والخطوات التالية |
| اجتماع قيادة / Leadership review | المؤشرات والقرارات المفتوحة |
| اجتماع عام / General meeting | الاجتماعات التي لا تنتمي إلى نوع محدد |

القالب ينشئ مسودة منظمة بالعنوان والملخص الأولي ومحور أو أكثر ونقطة متابعة. في النسخة الحالية تبدأ مسودة القالب محليًا؛ أما إنشاء صفحة جديدة في Notion مباشرة من القالب فهو توسعة مستقلة لم تُضف بعد.

## 10. المصادقة وقاعدة البيانات

تم استخدام Manus OAuth الجاهز في القالب. الخادم يقرأ المستخدم من سياق الطلب، بينما الإجراءات التي تصل إلى Notion مغلقة باستخدام `protectedProcedure`. الجلسة تعتمد على Cookie آمن ولا توجد معالجة يدوية للتوكن داخل React.

قاعدة البيانات الحالية تحتوي على جدول `users` الأساسي، ويتضمن المعرّف، و`openId`، والاسم، والبريد، وطريقة الدخول، والدور، وتواريخ الإنشاء والتحديث وآخر تسجيل دخول.

| ملاحظة مهمة | الحالة الحالية |
|---|---|
| تخزين المستخدمين | موجود في MySQL/TiDB عبر Drizzle |
| تخزين الاجتماعات | Notion أو المصدر المحلي الاحتياطي |
| جدول اجتماعات داخل قاعدة التطبيق | غير مضاف حاليًا |
| الصلاحيات | Manus OAuth + protected tRPC procedures |

## 11. الاختبارات والتحقق

تم تشغيل فحص TypeScript واختبارات Vitest بنجاح. النسخة الحالية تحتوي على **15 اختبارًا ناجحًا** موزعة على خمسة ملفات اختبار.

| مجموعة الاختبارات | ما تتحقق منه |
|---|---|
| `auth.logout.test.ts` | حذف Cookie الجلسة بشكل صحيح |
| `meeting.routes.test.ts` | مسارات التحضير والعرض |
| `meeting.utilities.test.ts` | الجاهزية، الترقيم، إعادة الترتيب، ومصدر البيانات |
| `notion.connection.test.ts` | وجود الأسرار وإمكانية الوصول إلى قاعدة Notion |
| `Home.test.tsx` | التفاعل الفعلي مع الواجهة، القوالب، اللغة، العرض، التخصيص، المزامنة، وملء الشاشة |

الأوامر المستخدمة للتحقق هي:

```bash
pnpm check
pnpm test
```

كما تم التحقق بصريًا من الواجهة على سطح المكتب والهاتف، بما في ذلك لوحة التخصيص المفتوحة، وضع العرض، ووضع العرض المضغوط.

## 12. الأوامر الأساسية للتشغيل

| الأمر | الوظيفة |
|---|---|
| `pnpm dev` | تشغيل بيئة التطوير مع المراقبة الحية |
| `pnpm check` | فحص TypeScript دون إنشاء ملفات |
| `pnpm test` | تشغيل جميع اختبارات Vitest |
| `pnpm build` | بناء الواجهة وتجميع الخادم للإنتاج |
| `pnpm start` | تشغيل نسخة الإنتاج بعد البناء |
| `pnpm format` | تنسيق الملفات عبر Prettier |
| `pnpm db:push` | توليد وترحيل مخطط Drizzle عند الحاجة |

## 13. ما هو جاهز وما يحتاج توسعة

### جاهز حاليًا

المشروع جاهز كمساحة تحضير وعرض اجتماعات، مع دعم عربي/إنجليزي، تكامل قراءة وحفظ مع Notion، قوالب، تخصيص واجهة، ملء شاشة، حالات تحميل واتصال، اختبارات، ونشر على نطاق المشروع.

### التوسعات المقترحة

1. إضافة إجراء `notionCreate` لإنشاء صفحة اجتماع جديدة في قاعدة Meeting Prep عند استخدام قالب.
2. إضافة حقول Notion إضافية مثل مدة الاجتماع، المالك، الأولوية، ورابط الاجتماع المرئي.
3. إضافة صلاحيات على مستوى الاجتماعات والمستخدمين إذا كان التطبيق سيُستخدم من فريق.
4. إضافة مزامنة دورية أو زر مزامنة يدوي مع معالجة تعارضات التحرير.
5. إضافة إعدادات مظهر محفوظة لكل مستخدم بدل حفظها محليًا في المتصفح.

## 14. الخلاصة

المشروع ليس مجرد صفحة عرض؛ هو تطبيق Full-stack متكامل يحتوي على React في الواجهة، Express وtRPC في الخادم، Manus OAuth للمصادقة، Drizzle/MySQL للمستخدمين، وNotion كمصدر اجتماعات خارجي. الطبقة المشتركة تفصل نموذج الاجتماع عن مصدر البيانات، مما يجعل النسخة الحالية قابلة للاستخدام فورًا وقابلة للتطوير نحو نظام اجتماعات جماعي أكثر اكتمالًا.
